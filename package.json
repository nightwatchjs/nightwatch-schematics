{
  "name": "nightwatchjs-schematics",
  "version": "0.0.1",
  "description": "Adds Nightwatchjs to an existing Angular CLI project",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:test": "tsc -p tsconfig.spec.json",
    "build:watch": "tsc -p tsconfig.json -w",
    "build:test:watch": "tsc -p tsconfig.spec.json -w",
    "test:unit": "jasmine src/**/*_spec.js",
    "test": "npm run build && jasmine src/**/*_spec.js",
    "clean": "git checkout HEAD -- sandbox && git clean -f -d sandbox",
    "execute": "npm run build && schematics .:ng-add",
    "execute:prod": "npm run build && schematics .:ng-add --dry-run=false",
    "link:sandbox": "npm link && cd sandbox && npm link $npm_package_name",
    "unlink:sandbox": "cd sandbox && npm unlink $npm_package_name && cd .. && npm unlink",
    "dev": "npm run build && npm run link:sandbox && cd sandbox && ng add nightwatchjs-schematics",
    "dev:build": "cd sandbox && npm link ../ && ng run sandbox:nightwatch-run"
  },
  "keywords": [
    "schematics",
    "builders",
    "nightwatch",
    "e2e",
    "testing",
    "nightwatchjs-schematic",
    "angular"
  ],
  "author": "Vaibhav Singh <singh.vaibhav2011@gmail.com>",
  "contributors": [],
  "license": "MIT",
  "builders": "./src/builders/builders.json",
  "bugs": "https://github.com/nightwatch/nightwatchjs-schematics/issues",
  "schematics": "./src/schematics/collection.json",
  "ng-add": {
    "save": "devDependencies"
  },
  "dependencies": {
    "@angular-devkit/architect": "^0.1200.1",
    "@angular-devkit/core": "^12.0.1",
    "@angular-devkit/schematics": "^12.0.1",
    "@schematics/angular": "^12.0.1",
    "typescript": "~4.1.2"
  },
  "devDependencies": {
    "@types/jasmine": "~3.5.0",
    "@types/node": "^12.11.1",
    "jasmine": "^3.5.0",
    "nightwatch": "^1.6.4"
  }
}
